
# TODO(sileht): move me in GRENADE_PLUGINRC of infra job
RUN_SMOKE=False
BASE_RUN_SMOKE=False
TARGET_RUN_SMOKE=False
ENABLE_TEMPEST=False
RUN_JAVELIN=False
VERIFY_RESOURCES=False

# FIXME(sileht): We shouldn't override this but grenade doesn't allow to not
# run nova/cinder/neutron/glance/swift/horizon/keystone. Overriding make the
# grenade plugins to work only in our gate not outside.
UPGRADE_PROJECTS="keystone"
DATABASES_TO_SAVE="keystone"

# NOTE(sileht): Grenade expect ebtables to be installed but because we didn't run
# neutron/nova into our job it's not
sudo apt-get install -y ebtables

# NOTE(sileht): Workaround LP#1581333
mkdir -p /opt/stack/old/devstack/files/images/

register_project_for_upgrade gnocchi
register_db_to_save gnocchi

devstack_localrc base enable_plugin gnocchi git://git.openstack.org/openstack/gnocchi
devstack_localrc base enable_service gnocchi-api gnocchi-metricd gnocchi-statsd
